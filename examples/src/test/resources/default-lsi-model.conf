model {
  // test data.
  textData = "example_text_corpus_data.csv"

  // idcolumns array of int.
  csvIdColumns = [0, 1]

  // binary serialization file to output
  targetSer = "lsiexample.ser"

  // zip archives with CSV for use in other tools for visualisation
  targetZip = "lsiexample.zip",

  // a query for testing default search.
  defaultQuery = ["http", "redirect", "error"]
}
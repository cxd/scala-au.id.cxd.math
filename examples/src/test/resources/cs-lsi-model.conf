model {
  // test data.
  textData = "/Users/cd/Nuance/data/csexample/tagging_data.csv"

  // idcolumns array of int.
  csvIdColumns = [0]

  // binary serialization file to output
  targetSer = "/Users/cd/Nuance/data/csexample/lsiexample.ser"

  // zip archives with CSV for use in other tools for visualisation
  targetZip = "/Users/cd/Nuance/data/csexample/lsiexample.zip",

  // a query for testing default search.
  defaultQuery = ["youth", "allowance"]
}